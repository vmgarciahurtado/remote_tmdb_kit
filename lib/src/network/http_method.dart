/// Métodos HTTP soportados por el servicio de red de la librería.
enum HttpMethod {
  /// Método HTTP GET.
  get,

  /// Método HTTP POST.
  post,

  /// Método HTTP PUT.
  put,

  /// Método HTTP DELETE.
  delete,

  /// Método HTTP PATCH.
  patch,
}
